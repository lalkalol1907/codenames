# Production with HTTPS (Ktor Netty SSL). Requires /certs/keystore.p12 mounted in the container.
# Passwords: SSL_KEYSTORE_PASSWORD, SSL_PRIVATE_KEY_PASSWORD (defaults to keystore password).
ktor:
  deployment:
    host: 0.0.0.0
    port: 8080
    sslPort: 443
  security:
    ssl:
      keyStore: /certs/keystore.p12
      keyAlias: codenames
      keyStorePassword: $SSL_KEYSTORE_PASSWORD
      privateKeyPassword: $SSL_PRIVATE_KEY_PASSWORD
      enabledProtocols:
        - TLSv1.2
        - TLSv1.3
  application:
    modules:
      - com.lalkalol.config.AppSettingsKt.configureAppSettings
      - com.lalkalol.config.HttpKt.configureHttp
      - com.lalkalol.config.DIKt.configureDependencyInjection
      - com.lalkalol.config.SecurityKt.configureSecurity
      - com.lalkalol.config.DatabaseKt.configureDatabase
      - com.lalkalol.config.HealthKt.configureHealth
      - com.lalkalol.config.TemplatesKt.configureTemplates
      - com.lalkalol.config.SessionsKt.configureSessions
      - com.lalkalol.web.WebSocketsKt.configureWebSockets
      - com.lalkalol.config.RoutingKt.configureRouting

app:
  environment: prod
  publicUrl: "https://example.com"
  secureCookies: true
  sessionSecret: "replace-with-long-random-secret"
  exposeApiDocs: false

database:
  url: "jdbc:postgresql://localhost:5432/codenames"
  driver: org.postgresql.Driver
  user: codenames
  password: codenames
  poolSize: 10
